---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rrrrrrrrrr                  # Ingress name
  labels:
    app: rrrrrrrrrr
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "rrrrrrrrrr.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rrrrrrrrrr               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
